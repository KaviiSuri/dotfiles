# Match the existing home-row navigation style first.
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

bind z resize-pane -Z
bind r source-file ~/.config/tmux/tmux.conf \; display-message "tmux config reloaded"

# Window switching should map cleanly to tab-like behavior.
bind 1 select-window -t :=1
bind 2 select-window -t :=2
bind 3 select-window -t :=3
bind 4 select-window -t :=4
bind 5 select-window -t :=5
bind 6 select-window -t :=6
bind 7 select-window -t :=7
bind 8 select-window -t :=8
bind 9 select-window -t :=9

# Session and window pickers are first-class navigation tools.
bind w choose-window
bind s choose-tree -s -O time

# tmuxp template shortcuts (Alt+1, Alt+2)
bind -n M-1 run-shell "tmuxp load default-coding"
bind -n M-2 run-shell "tmuxp load simple"
